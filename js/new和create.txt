1.通过new和create出来的对象异同
  # new
   > new的过程实质就是__proto__和prototype的连接
   >> 最后通过显示绑定this赋值
   >>> 结果就是new出来的对象里有相应的属性，并且继承了原型对象
   
  ## create 
   > create的过程实质就是内部新创一个空对象，并把空对象的原型指向传入的对象
   >> 最后return new 新对象
   >>> 结果是最后new出来的对象里面没有任何属性，但是他的隐式原型(__proto__)上有相应的属性