1.同异步执行顺序
  # 在执行栈按顺序执行所有的同步任务
  ## 与此同时异步任务等待响应，并把响应的方法推入任务队列
  ### 查看任务队列里的事件并且推入执行栈执行
  #### 循环二,三步
  
2.Vue更新DOM的执行状态
  # Vue等待内部数据更新
  ## 数据更新并把相应的事件推入任务队列   （如果有的话，eg：{{ data }} 等同于 dom.innerText = data）
  ### 查看任务队列里的事件并且推入执行栈执行更新ODMM
  #### 循环二,三步
  
3.this.nextTick()
  # 在Vue实例内部改变数据，这时候是获取不到更新的DOM 因为这时候只是把相关事件推入任务队列
  ## nextTick内部传入一个回调函数,执行的时机是在DOM更新以后(在执行栈执行过后)
  
