1.sync的应用需求
  # 当子组件需要父组件的某个值并且可以更改从父组件传入的值时
  
2.用法
  # 在父组件的子组件标签中v-bind:自定义数据名字.sync="需要传入的数据名字"
  ## sync是一个语法糖,真是做法是 @update:需要传入的数据名字=一个带形参更改父组件的函数
    > eg: function (value){ this.需要传入的数据名字 = value }
  ### 通过this.$meit()触发

3.总结
  # 和props,emit()的功能一致，但是可以稍微减少代码量